query GetCartSummary($site: SiteEnum!) {
  currentUser {
    shoppingCart(site: $site) {
      id
      items {
        id
        quantity
        variant {
          id
        }
      }
    }
  }
}
