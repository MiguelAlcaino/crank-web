query GetRemainingCredits {
  currentUser {
    remainingCredits {
      ... on RemainingCreditsSuccess {
        credits
      }
      ... on ClientNotFoundInMindbody {
        code
      }
    }
  }
}
