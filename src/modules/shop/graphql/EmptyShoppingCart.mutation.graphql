mutation EmptyShoppingCart($site: SiteEnum!) {
    emptyShoppingCart(site: $site) {
        __typename
        ... on ShoppingCart {
            ...ShoppingCartFields
        }
        ... on ShoppingCartNotFound {
            code
        }
    }
}
